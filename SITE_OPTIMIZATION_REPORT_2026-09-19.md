# Cowin Supply 全站优化与验收记录

日期：2026-09-19。范围：公开网站、询盘接口/持久化/邮件通知、导航与图片、Google 收录技术检查。保留既有产品及业务功能。

## 已实施

- 静态页和动态页使用统一页头、页脚；桌面下拉支持移入保持、移出延迟收起、选择后收起，手机使用点击展开；补充焦点、Escape 和页面切换关闭行为。
- 页脚真实电话链接 WhatsApp；公司位置链接用户提供的 Google Maps。未把未经验证的街道地址当作真实公司地址。
- 首页产品卡从产品目录读取真实图片、名称和链接，新闻卡读取已发布新闻；修正分类图片/标签不匹配及模板虚构统计。
- 清理场景图内残留的模板文字，替换为现有清晰场景素材；商品图保持实际商品素材，不以 AI 图片替代商品实拍。应用/工厂示意图不作为工厂认证或实拍证据。
- 产品 buyer-briefing 新闻优先采用其关联商品图片，防止曲线锯文章展示砂光机；列表、详情和首页使用同一内容读取逻辑。
- 关于我们和联系页清理建站说明、失实图片标签及旧地址文案，改为面向买家的选型、应用和询价信息。
- 询盘提交增加异步状态、明确成功/失败反馈、防重复提交、失败保留输入；服务端校验空白/非法输入，先持久化再异步发邮件，配置邮件连接超时。
- 浮动 WhatsApp 按钮移至底部，降低遮挡；统一响应式导航、卡片、表单及图片显示规则。
- 增加只读检查工具 tools/audit-public-pages.mjs，检查 sitemap 页面、统一导航/页脚和 img 资源；不包含 CSS 背景图的自动扫描，背景图另作目视检查。

## 已验证的事实

1. 首轮生产检查：96 个唯一 sitemap 页面，HTTP/页头/页脚均通过；60 个独立 img 资源全部成功返回图片。
2. 技术可索引检查：96/96 通过（响应、canonical、robots 等），不等于 Google 已收录。
3. 浏览器主要模板：主页、产品列表、关于、联系、新闻列表、博客列表、分类页、商品详情；390、768、1440 宽度共 24 个组合，无页面横向溢出，统一页头/页脚各一个。桌面/手机导航选择后关闭已验证。
4. 三笔明确标记的生产测试询盘分别从桌面联系页、手机联系页、商品详情页提交；数据库均保存，通知状态均为 sent，商品型号关联正确。后台使用同一询盘表；未以后台界面截图代替数据库核验。
5. sent 表示邮件服务接受发送，不保证收件箱最终投递。原流程并非完全不可用；修复重点是反馈体验、失败恢复和邮件等待耦合，不能把所有低转化归因于表单故障。
6. 已按精确测试 ID 清理 3 条询盘、3 条通知、6 条测试活动记录；未删除真实客户询盘。测试邮件不能从收件人的邮箱中撤回。
7. 最近 30 天统计中，150 条访问事件至少有 36 条明显机器人/无头浏览器标识；不能将请求数视为真实访客数或据此计算可靠转化率。

## Google 实际状态和限制

- 持久化任务记录显示：北京时间 2026-09-19 12:00:25 sitemap 提交成功，Google API 返回 204。提交任务包含 98 个条目，去重后为 96 个页面。
- 最近 URL Inspection 任务于当日 12:20 完成，抽查 10 个 URL：9 PASS、1 NEUTRAL，无 API 错误。
- 9 月 9–19 日已有检查记录按 URL 保留最新一次：89 个 URL，其中 62 已收录、22 已发现未收录、2 已抓取未收录、3 Google 尚未知晓。不是实时完整 Pages 报表，也不能理解为今天全站总收录量。
- 主动提交已执行，但提交 sitemap 不保证或强制收录。保留现有正常提交机制；本轮通过首页最新新闻链接和准确内容/配图改善内部发现路径。
- 未取得 Google Search Console 完整 Pages 报表的实时界面数据；不承诺某个日期全部收录。后续应对未收录 URL 按内容重复度、独有价值、内部链接和 Google 重新抓取情况逐条复查。

## 验收边界

- 不能将所有原始商品图承诺为高像素素材；部分供应商原图约 523–1000 像素，遵循合适展示尺寸，不用简单放大伪造高清。若需大幅高清展示，应补充真实高分辨率源图。
- 本轮覆盖现有英文页面和移动/桌面布局，不代表新增并验收了所有语言版本。
- 现有收录记录和流量数据有时间窗口、抽样及机器人影响；无证据证明 Google 抓取或所有询盘曾整体中断。
- 构建、代码校验通过后推送 main，由 Vercel 生产部署；交付时另核实最终部署 READY、域名及关键修正上线。
